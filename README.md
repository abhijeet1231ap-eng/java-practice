# Java Practice

A Java practice repository covering Core Java, Object-Oriented Programming (OOP), and Java Collections Framework concepts through hands-on examples.

---

## 📂 Files

### Calculator.java

A console-based calculator built to practice user input, conditional logic, and arithmetic operations.

**Concepts Covered**

* Scanner class
* If-Else statements
* Arithmetic operators
* User input handling

---

### Practice.java

Explores Java Enums, Object class methods, and Interface concepts.

**Enums**

* Simple enums (`Level`)
* Enum with constructor and fields (`Day`)
* Enum with abstract methods (`TrafficLight`)
* Enum with data and behavior (`Planet`, `Season`)

**Object Class**

* `toString()`
* `equals()`

**Interfaces**

* Basic interfaces (`MediaPlayer`, `Shape`, `Employee`, `Greetable`, `Trusted`)
* Default methods
* Functional interfaces (`@FunctionalInterface`)
* Marker interfaces
* `instanceof` usage

---

### Practice4.java

Hands-on practice with Java Collections Framework.

**Collections Covered**

* `ArrayList`
* `LinkedList`
* `HashSet`
* `HashMap`

**Operations**

* Adding elements
* Removing elements
* Retrieving elements
* Updating values
* Collection traversal using loops

---

### Practice5.java

Focused practice on Iteration in Java Collections.

**Iteration Techniques**

* Normal `for` loop
* Enhanced `for-each` loop
* `Iterator`
* `ListIterator`

**Iterator Methods**

* `iterator()`
* `hasNext()`
* `next()`
* `remove()`

**ListIterator Methods**

* `hasPrevious()`
* `previous()`
* `add()`
* `set()`
* `nextIndex()`
* `previousIndex()`

**Exception Handling**

* `ConcurrentModificationException`
* `IllegalStateException`
* `NoSuchElementException`

---

### Practice6.java

Practice of sorting collections using Comparable and Comparator.

**Comparable**

* Implementing `Comparable<T>`
* `compareTo()` method
* Natural ordering
* Sorting custom objects
* Ascending and descending sorting

**Comparator**

* Implementing `Comparator<T>`
* `compare()` method
* Custom sorting logic
* Sorting by salary
* Sorting by name
* Multiple sorting strategies

---

## 🚀 Concepts Covered

### Core Java

* Variables & Data Types
* Operators
* Conditional Statements
* Loops
* Methods
* Classes & Objects

### Object-Oriented Programming

* Encapsulation
* Inheritance
* Polymorphism
* Abstraction
* Interfaces
* Enums
* Object Class

### Java Collections Framework

* ArrayList
* LinkedList
* HashSet
* HashMap
* Iterator
* ListIterator
* Comparable
* Comparator
* Collections Utility Class (`Collections.sort()`)

---

## ▶️ How to Run

```bash
javac <FileName>.java
java <FileName>
```

---

## 🎯 Learning Goal

This repository documents my Java learning journey through practical coding examples, covering Core Java, OOP, and the Java Collections Framework. New topics and examples are added regularly as I continue learning.
